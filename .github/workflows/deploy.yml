- name: Deploy to Firebase
  run: |
    echo '${{ secrets.GOOGLE_APPLICATION_CREDENTIALS_JSON }}' > /tmp/sa.json
    export GOOGLE_APPLICATION_CREDENTIALS=/tmp/sa.json
    firebase deploy --only hosting --project jottieplans
